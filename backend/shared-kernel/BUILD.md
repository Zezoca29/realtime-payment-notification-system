# Dependência do shared-kernel via instalação local
# Execute antes de buildar os módulos:
#
#   cd backend/shared-kernel
#   mvn install -DskipTests
#
# Em seguida rode os demais módulos normalmente.
